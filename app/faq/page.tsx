'use client'; import { useState } from 'react'; import Nav from '@/components/Nav';
const faqs: Record<string, { q: string; a: string }[]> = { Individual: [{ q: 'How many photos?', a: '10-20 clear selfies.' }, { q: 'Refunds?', a: '100% money-back guarantee within 14 days.' }], Free: [{ q: 'Sign up?', a: 'No account needed.' }] };
export default function FAQPage() { const [open, setOpen] = useState<Record<string, boolean>>({}); return (<div className="min-h-screen bg-slate-50 font-sans text-slate-900"><Nav showBack /><div className="max-w-3xl mx-auto px-6 py-16"><h1 className="text-4xl font-bold text-center mb-12">FAQ</h1>{Object.entries(faqs).map(([sec, items]) => (<div key={sec} className="mb-10"><h2 className="text-sm font-bold text-blue-600 uppercase tracking-wider mb-4">{sec}</h2><div className="space-y-2">{items.map((faq, idx) => { const k = `${sec}-${idx}`; return (<div key={k} className="bg-white rounded-xl border border-slate-200 overflow-hidden"><button onClick={() => setOpen(p => ({...p, [k]: !p[k]}))} className="w-full text-left px-6 py-4 font-semibold flex justify-between">{faq.q}<span>{open[k] ? '-' : '+'}</span></button>{open[k] && <div className="px-6 pb-4 text-slate-600 border-t border-slate-100">{faq.a}</div>}</div>); })}</div></div>)}</div></div>); }
